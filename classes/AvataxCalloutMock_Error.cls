@isTest
global class AvataxCalloutMock_Error implements WebServiceMock{
    global void doInvoke(Object stub,
           Object request,
           Map<String, Object> response,
           String endpoint,
           String soapAction,
           String requestName,
           String responseNS,
           String responseName,
           String responseType){
               // Create response element from the autogenerated class.
               // Populate response element.
               // Add response element to the response parameter, as follows:
               avataxAvalaraComServicesAlt.GetTaxResponse_element responseElement = 
                   new avataxAvalaraComServicesAlt.GetTaxResponse_element();
               avataxAvalaraComServicesAlt.GetTaxResult result = 
                   new avataxAvalaraComServicesAlt.GetTaxResult();
               result.ResultCode = 'Error';
               result.Messages = new avataxAvalaraComServicesAlt.ArrayOfMessage();
               result.Messages.Message = new List<avataxAvalaraComServicesAlt.Message>();
               avataxAvalaraComServicesAlt.Message message = new avataxAvalaraComServicesAlt.Message();
               message.Details = 'Test Details';
               message.RefersTo = 'Test Refers To';
               message.Source = 'Test Source';
               result.Messages.Message.add(message);
               responseElement.GetTaxResult = result;
               response.put('response_x', responseElement);
    }
}